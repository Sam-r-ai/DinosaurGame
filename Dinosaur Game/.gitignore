# Godot-specific ignores
export_presets.cfg
export.cfg
import/
*.import
*.bin
 # If you want to ignore all cfg files, though be cautious as some cfg files may need to be included
# Specific editor configuration files
#*.cfg

**/editor_layout.cfg
**/project_metadata.cfg

**/filesystem_cache*
**/uid_cache.bin

# Godot generated files and folders
.cache/
# Ignore Godot user-specific settings
*.godot/*.autoloads
*.godot/app_userdata/
# OS-specific ignores
.DS_Store
*.swp
*~
# Ignore specific project files that hold user-specific settings
override.cfg
# Add-ons you might not want to include
addons/
# If you're using a virtual environment for Python or other languages
venv/
.env
# Ignore large files that you don't want to store in Git (consider using Git LFS)
*.psd
*.blend

# Godot editor settings
.editor_layout.cfg
.script_editor_cache.cfg
.global_script_class_cache.cfg
.godot\editor\script_editor_cache.cfg
